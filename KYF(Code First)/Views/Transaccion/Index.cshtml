@model IEnumerable<KYF_Code_First_.Models.Transaccion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Motivo.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarjeta.NumeroTarjeta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Motivo.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarjeta.NumeroTarjeta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransaccionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransaccionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransaccionID })
        </td>
    </tr>
}

</table>
